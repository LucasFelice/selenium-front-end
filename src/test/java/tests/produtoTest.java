package tests;

import core.baseTest;
import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.WebDriver;

import static core.driverFactory.getDriver;

public class produtoTest extends baseTest {

    @Test
    public void addProdutoAoCarrinhoPorSearchProduct() {
       // menu.Products();
        //Assert.assertEquals("ALL PRODUCTS", produto.validarTituloAllProducts());
        //String nomeproduto = produto.obterNomeProduto();
       // produto.setSearch(nomeproduto);
       // produto.submitSearch();
        ////Assert.assertTrue("nomeProduto", produto.);
        //produto.addToCart();
        //getDriver().switchTo().frame("//div[@class='features_items']");
        //Assert.assertEquals(
          //      "Your product has been added to cart.", produto.obterMsgProductAdcToCart());
        //produto.viewCart();
    }

















}
